language: php
  - 5.6

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script: vendor/bin/phpunit

matrix:
  allow_failure:
    - php: 5.6
  fast_finish: true

notifications:
  on_success: never
  on_failure: always

sudo: false
